the destCity ; DateTime entities
a book title ; author ; credit card information ; the address that the book will be shipped to
destCity ; ArrivalDateTime ; departDateTime
a picture of a barcode of a product ; his current local position
his PatientID ; DeviceAddress; ; a blood pressure; a level of Warning; a Warning level
a patient ID; a patient ID; a patient ID; a treatment ID; a medication ID;
the hotel name ; city ; state information
the zip code ; food preference
the current location ; food preference
the start ; destination addresses
node
a string input representing the file system in the explained format
an integer array of digits ; digits
a sequence of words written in the alien language ; the order of the alphabet ; words
A ; B ; input
the string p
an integer N
the locations ; height of all the buildings as shown on a cityscape photo
array
program
the employee information of a company ; an employee id
a string expression representing a Lisp-like expression
two 1s
an integer array nums
a chemical formula
a string s ; string
two positive integers L ; R
array
the Teemo's attacking ascending time series towards Ashe ; the poisoning time duration per Teemo's attacking
a square at location in the grid ; a color ; color
a string s
a list of unique words ; list
equation
an integer array destination ; an integer k
a list of strings
an input array nums
the total number of courses numCourses ; a list of the prerequisite pairs
an integer array arr ; a target value target ; array
node ; node
A string S of lowercase English letters
a list of daily temperatures T
the root node of a binary search tree ; a value ; value
an expression such as expression = " e + 8-a + 5 " ; an evaluation map

all the cities ; flights ; together with starting city src ; the destination dst
string arrays queries ; words
several projects
projects
parameters
you can eliminate at most k obstacles
certain items
a string s
the arrays of startTime ; endTime ; profit
several matchsticks the girl has ; represented with their stick length
two non-empty linked lists representing two non-negative integers
number N in base-10
list mines
a string representing n
an array consisting of n integers ; length k that has the maximum average value
an array of integers with possible duplicates ; target number ; target number
a printNumber method to output an integer ; program
the root of a Binary Search Tree ; a target number k ; target
the next click position among all the unrevealed squares
an expression representing a set of words under the given grammar
2D-array
a string s
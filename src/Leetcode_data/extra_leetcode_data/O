result as a list of indices representing the starting position of each interval, lexicographically smallest one
/
sum of num1 and num2
maximum width of a ramp in A, 0
True, False
three numbers whose product is maximum, maximum product
water cell, distance
next greater number for it in the second array, -1
difference between the product of its digits and the sum of its digits
matrix answer
/
/
whether these points are a boomerang
indices of the two numbers
subsequence with minimum size, the subsequence with the maximum total sum of all its elements
true
maximum score
/
/
/
/
/
answer list of booleans
/
/
minimum number of times you need to do this in order to make all the computers connected, -1
number of triplets formed ( type 1 and type 2 ) under the following rules
/
/
array of the squares of each number
/
largest V
empty string
sum of divisors of the integers in that array
big countries' name , population and area 
/
/
/
length of the shortest such clear path from top-left to bottom-right, -1
its complement number
/
/
all possible times the watch could represent
all elements of the matrix in diagonal order
maximal network rank of the entire infrastructure
length longest chain which can be formed
32
/
integer part of the result 
level order traversal of its nodes' values
one of the keys with maximal value, empty string
the number of requests that has happened in the past 3000 milliseconds, number of requests that have happened in the inclusive range
smallest K for which A and B are K-similar
list of every possible self dividing number
minimum integer you can obtain also as a string
/
2
/
/
its head
nesting depth of s
number of tuples i , j , k
/
/
/
route that requires the minimum effort
kth string of this list, empty string
/
/
/
/
answer modulo
bitwise AND of all numbers in this range
answers to all queries, -1
True, False
minimum number of moves to transform the given array nums into a zigzag array 
/
" buzz "
/
true
/
/
subsequence of the array whose sum of elements is strictly greater than the sum of the non included elements in such subsequence
empty string
first such match in the wordlist
/
maximum value of
total number of friend circles among all the students
it in string format
true
any answer array that satisfies this condition
/
/
number of islands
the answer in any order
3 as the starting index
True
root node reference ( possibly updated ) of the BST
/
minimum number of semesters to take all courses
/
/
your answer modulo
average value of the nodes on each level
/
value of T n
its zigzag level order traversal as
-1, left-most pivot index
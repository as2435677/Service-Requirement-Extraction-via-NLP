result as a list of indices representing the starting position of each interval ( 0-indexed ), lexicographically smallest one

sum of num1 and num2
maximum width of a ramp in A, 0
True if the array is good, False
three numbers whose product is maximum and return the maximum product
water cell such that its distance to the nearest land cell is maximized and return the distance
-1
difference between the product of its digits and the sum of its digits
matrix answer where each answer [ i ] [ j ] is the sum of all elements mat


whether these points are a boomerang
indices of the two numbers such that they add up to the target
subsequence with minimum size, the subsequence with the maximum total sum of all its elements
true
maximum score you can obtain





answer list of booleans , where answer [ i ] is true if and only if queries [ i ] matches the pattern


minimum number of times you need to do this in order to make all the computers connected, -1
number of triplets formed ( type 1 and type 2 ) under the following rules


array of the squares of each number , also in sorted non-decreasing order

largest V
empty string
sum of divisors of the integers in that array that have exactly four divisors
big countries' name , population and area



length of the shortest such clear path from top-left to bottom-right, -1
its complement number


all possible times the watch could represent
all elements of the matrix in diagonal order
maximal network rank of the entire infrastructure
length longest chain which can be formed
32

integer part of the result
level order traversal of its nodes' values
one of the keys with maximal value, empty string
number of requests that has happened in the past 3000 milliseconds
smallest K for which A and B are K-similar
list of every possible self dividing number , including the bounds if possible
minimum integer you can obtain also as a string

2


its head
nesting depth of s
number of tuples i , j , k




kth string of this list or return an empty string




answer modulo
bitwise AND of all numbers in this range , inclusive
answers to all queries, -1
True if its possible otherwise return False
minimum number of moves to transform the given array nums into a zigzag array

" buzz "

true if and only if you can provide every customer with correct change 


subsequence of the array whose sum of elements is strictly greater than the sum of the non included elements in such subsequence
empty string
first such match in the wordlist

maximum value of
total number of friend circles among all the students
it in string format
true , otherwise return false
any answer array that satisfies this condition


number of islands
the answer in any order
3
True if and only if there exists a sequence of moves to transform one string to the other
root node reference ( possibly updated ) of the BST

minimum number of semesters to take all courses


answer modulo
average value of the nodes on each level in the form of an array

value of T n
its zigzag level order traversal
-1, left-most pivot index



[ 1,10,11,12,13,2,3,4,5,6,7,8,9 ]


4
sum of every tree node's tilt
root of the trimmed binary search tree
-1
0
sum of values of nodes with even-valued grandparent
-1

1
3

3
median sliding window as [ 1,-1,-1,3,5,6 ]




list containing the IDs ( 0-indexed ) of the busiest server
node with value 2



total Hamming distance between all pairs of the given numbers
minimum number of boats to carry every given person

modified image


any beautiful array A


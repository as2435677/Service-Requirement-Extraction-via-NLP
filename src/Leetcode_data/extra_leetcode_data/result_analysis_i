

第一個element多represented



少containing後細節(option)


取出的element不完整





因take取出多餘的元素

取出元素有錯

少representing後細節且取出許多多餘的元素

少了of後細節




取出正確元素,但排列方式錯誤會造成user誤會
沒有取出元素
多取出一個元素以及取出正確元素但把細節加入後導致錯誤
少sorted後細節






少一個元素看起來兩個元素合併了



多一個一樣的component












取出錯誤元素




第二個元素多了array

少represented後的as細節

多取出了redundant elements但有取出正確元素

少一個element



多一個redundant elements但有取出正確元素










取出多餘元素,正確元素少了with後細節

把兩個元素合成一個,但看起來是正確的



consisting後細節為tree的種類(重要)







取出的元素格式錯誤且多了一個element


少了一個element

多一個redundant element但有取出正確元素
取出相同元素但是正確的

consisting_digits重複兩次,但結果正確





